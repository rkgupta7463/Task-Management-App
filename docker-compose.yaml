version: "3.9"

services:
  mysql:
    image: mysql:8.0
    container_name: task_manager
    restart: always
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: 1234
      MYSQL_DATABASE: task_mang_db
      MYSQL_USER: app_user
      MYSQL_PASSWORD: app_pass
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
